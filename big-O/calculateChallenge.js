function calculateChallenge(input) {
    const a = 5     // O(1)
    const b = 10    // O(1)
    const c = 50    // O(1)
    for (let i = 0; i < input; i++) {
        const x = i + 1;    // O(n)
        const y = i + 2     // O(n)
        const z = i + 3     // O(n)
    }

    for (let i = 0; i < input; i++) {
        const p = j * 2;    // O(n)
        const q = j * 2;    // O(n)
    }
    const whoAmI = "I dont know"    // O(1)
    console.log(whoAmI)     // O(1)


    // ______
    // total Big O: O(4+5n)
}

calculateChallenge([1,2,3,4])